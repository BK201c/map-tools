//electron
import {
  app,
  protocol,
  BrowserWindow,
  Menu,
  ipcMain,
  clipboard,
  ipcRenderer,
} from "electron";

export { app, protocol, BrowserWindow, Menu, ipcMain, clipboard, ipcRenderer };
